============
Installation
============

At the command line::

    $ easy_install django-drip-marketing

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-drip-marketing
    $ pip install django-drip-marketing
